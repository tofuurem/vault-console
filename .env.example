# Public, non-secret build-time defaults. Never put a Vault token here.
VITE_VAULT_ADDR=https://vault.example.com

# Use / for a dedicated hostname, or e.g. /console/ for a subpath deployment.
BASE_PATH=/
